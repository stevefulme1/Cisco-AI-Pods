---
# =========================================================
#   Templates -> Server - Variables
# =========================================================
intersight:
  configure:
    common:
      templates:
        server:
          - bios_policy: VirtualizationM8AmdTpm
            boot_order_policy: m2-vkvm-cimc-uefi
            create_template: true
            firmware_policy: 2025-09
            imc_access_policy: aci-kvm
            ipmi_over_lan_policy: ipmi
            lan_connectivity_policy: common
            local_user_policy: users
            name: ControllerVirtualizationM8AmdTPM
            power_policy: server
            serial_over_lan_policy: sol
            snmp_policy: snmp
            server_family: All
            storage_policy: M2Raid
            target_platform: FIAttached
            thermal_policy: server
            uuid_pool: uuid
            virtual_kvm_policy: vkvm
            virtual_media_policy: vmedia
