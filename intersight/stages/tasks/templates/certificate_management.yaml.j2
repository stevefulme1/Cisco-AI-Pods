---
{{
  item.1
  | merge_schema_defaults(intersight_schema, 'intersight.certificate_management')
  | to_module_params('intersight_certificate_management_policy')
  | combine({
      'description': item.1.description | default(omit),
      'name': policy_name_prefix.certificate_management | default(policy_name_prefix.default | default(''))
               ~ item.1.name
               ~ policy_name_suffix.certificate_management | default(policy_name_suffix.default | default('')),
      'organization': item.0.key
    })
}}
