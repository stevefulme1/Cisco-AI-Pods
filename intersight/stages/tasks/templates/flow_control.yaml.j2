---
description: "{{ item.1.description | default(omit) }}"
name: "{{ policy_name_prefix.flow_control | default(policy_name_prefix.default | default('')) ~ item.1.name ~ policy_name_suffix.flow_control | default(policy_name_suffix.default | default('')) }}"
organization: "{{ item.0.key }}"
priority_flow_control_mode: "{{ item.1.priority | default('auto') }}"
receive_direction: "{{ item.1.receive | default('Disabled') }}"
send_direction: "{{ item.1.send | default('Disabled') }}"
