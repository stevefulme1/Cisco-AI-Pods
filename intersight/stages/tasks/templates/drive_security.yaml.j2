---
{{
  item.1
  | merge_schema_defaults(intersight_schema, 'intersight.drive_security')
  | to_module_params('intersight_drive_security_policy')
  | combine({
      'description': item.1.description | default(omit),
      'name': policy_name_prefix.drive_security | default(policy_name_prefix.default | default(''))
               ~ item.1.name
               ~ policy_name_suffix.drive_security | default(policy_name_suffix.default | default('')),
      'organization': item.0.key
    })
}}
