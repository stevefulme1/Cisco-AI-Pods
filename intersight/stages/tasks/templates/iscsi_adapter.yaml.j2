---
connection_time_out: "{{ item.1.tcp_connection_timeout | default(15) }}"
description: "{{ item.1.description | default(omit) }}"
dhcp_timeout: "{{ item.1.dhcp_timeout | default(60) }}"
lun_busy_retry_count: "{{ item.1.lun_busy_retry_count | default(15) }}"
name: "{{ policy_name_prefix.iscsi_adapter | default(policy_name_prefix.default | default('')) ~ item.1.name ~ policy_name_suffix.iscsi_adapter | default(policy_name_suffix.default | default('')) }}"
organization: "{{ item.0.key }}"
