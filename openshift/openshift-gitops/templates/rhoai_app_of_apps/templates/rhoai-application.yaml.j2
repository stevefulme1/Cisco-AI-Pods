{{- if .Values.enableRhoaiApp }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhoai-deployment
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  destination:
    name: in-cluster
    namespace: openshift-gitops
  source:
    path: openshift/openshift-gitops/helm/rhoai-stack/
    repoURL: '{{ openshift.operators.openshift_gitops.gitops_repo_url }}'
    targetRevision: main
    helm:
      valuesObject:
        {{- .Values.configuration.RhoaiApp | toYaml | nindent 8 }}
  project: default
  syncPolicy:
    automated:
      prune: {{ openshift.operators.openshift_gitops.prune | default('false') }}
      selfHeal: {{ openshift.operators.openshift_gitops.self_heal | default('false') }}
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    managedNamespaceMetadata:
      labels:
        argocd.argoproj.io/managed-by: openshift-gitops
{{- end }}